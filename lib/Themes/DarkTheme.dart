import 'package:whatsapp/Export.dart';

ThemeData darkTheme = ThemeData(
    brightness: Brightness.dark,
    colorScheme: const ColorScheme.dark(
      background: kBackgroundColor,
        primary: kMainColor,
      secondary: kTextWhiteColor,
        tertiary: kTetiaryColor
    )
);